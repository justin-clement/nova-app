import styles from "../styling/novaheader.module.css"

function NovaHeader() {

    return (
        <div className={styles.headerContainer}>
            <p className={styles.title}>supernova</p>
        </div>
    )
};

export default NovaHeader;